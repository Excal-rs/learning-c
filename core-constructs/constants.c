
#include <stdio.h>

int main(){
// to create a constant preseed the datatype with const
	const float PI = 3.14; // it is convention to name constants in all caps
	//PI = 1.11111; //running this would lead to a error
	printf("%f", PI);


}
